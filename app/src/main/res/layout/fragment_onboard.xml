<layout xmlns:app="http://schemas.android.com/apk/res-auto">
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.splash.onboard.OnboardFragment">

    <ImageView
        android:id="@+id/bg"
        android:background="@drawable/bg_onboard"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="visible"/>

    <ImageView
        android:id="@+id/imgIcon"
        app:layout_constraintTop_toBottomOf="@+id/tvTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:src="@drawable/ic_onboard_1"
        android:layout_marginBottom="150dp"
        android:layout_height="0dp"/>

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/gui12"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintGuide_percent="0.06"
        android:layout_width="match_parent"
        android:layout_height="1dp"/>

    <TextView
        android:id="@+id/tvTitle"
        android:text="@string/our_zomj_stickers_add_dynamic_hues"
        android:gravity="center"
        android:paddingEnd="25dp"
        android:fontFamily="@font/inter_bold"
        android:textSize="24sp"
        android:textColor="@color/gnt_black"
        android:paddingStart="25dp"
        app:layout_constraintTop_toTopOf="@+id/gui12"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ViewStub
        android:id="@+id/viewStub"
        android:layout="@layout/layout_select_theme_default"
        android:inflatedId="@id/viewStub"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>