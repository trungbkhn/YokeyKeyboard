<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="40dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <androidx.cardview.widget.CardView
        android:id="@+id/cvItemTabSticker"
        android:foreground="?android:attr/selectableItemBackground"
        app:cardCornerRadius="4dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#00ffffff"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="5dp"
        android:layout_marginStart="5dp"
        android:layout_width="30dp"
        android:layout_height="30dp">
        <ImageView
            android:id="@+id/imgItemTabSticker"
            android:background="@drawable/bg_stroke_corner_4"
            android:src="@drawable/ic_recent"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
    </androidx.cardview.widget.CardView>

    <View
        android:background="#ffe837"
        android:id="@+id/viewLine"
        android:layout_marginTop="3dp"
        android:layout_marginBottom="1dp"
        app:layout_constraintTop_toBottomOf="@id/cvItemTabSticker"
        app:layout_constraintEnd_toEndOf="@id/cvItemTabSticker"
        app:layout_constraintStart_toStartOf="@id/cvItemTabSticker"
        android:layout_width="0dp"
        android:layout_height="1dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>